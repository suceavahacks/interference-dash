```bash
git clone https://github.com/suceavahacks/interference-dash.git && cd interference-dash && python3 -m venv venv && source venv/bin/activate && pip install pygame pydub && python3 main.py
```

run, jump, dodge zombies, while collecting energy drinks that give you speed but cause signal interference that "glitches" reality